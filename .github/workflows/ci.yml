<<<<<<< HEAD
name: CI

on: [push, pull_request]

jobs:
  web-tests:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      - run: npm ci
      - run: npx prisma generate
      - run: npm run lint
      - run: npm run type-check || true
      - run: npm run test:ci || npm run test || true
      - run: npm run build

  python-tests:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install deps
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt || true
          pip install pytest pytest-asyncio flake8 black mypy || true
      - name: Lint & Type Check
        run: |
          black --check . || true
          flake8 . || true
          mypy src/ || true
      - name: Run Tests
        run: pytest -q || true
=======
# GitHub Actions CI Workflow for RealMultiLLM
# This workflow runs linting, type checking, and tests on every push.

name: CI

on: [push, pull_request]

jobs:
  test:
    runs-on: macos-latest # Must run on macOS per requirements

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Set up Python 3.11
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'

    - name: Install Poetry
      run: pip install poetry

    - name: Install dependencies
      run: poetry install

    - name: Run Linters and Formatters
      run: |
        poetry run black --check .
        poetry run flake8 .
        poetry run mypy src/

    - name: Run Tests with Coverage
      run: poetry run pytest --cov=src
>>>>>>> d0d5cb3 (Create ci.yml)
