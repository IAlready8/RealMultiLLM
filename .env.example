# RealMultiLLM Environment Configuration
# Copy this file to .env.local and fill in your actual values

# =============================================================================
# Core Application Configuration
# =============================================================================

# Environment (development, production, test)
NODE_ENV=development

# Next.js Configuration
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your-nextauth-secret-here-must-be-at-least-32-characters-long-and-secure

# =============================================================================
# Database Configuration
# =============================================================================

# SQLite for development (default)
DATABASE_URL="file:./dev.db"

# PostgreSQL for production (uncomment and configure)
# DATABASE_URL="postgresql://username:password@host:5432/database?schema=public"

# =============================================================================
# Security Configuration (CRITICAL - REQUIRED FOR PRODUCTION)
# =============================================================================

# Master encryption key for API keys and sensitive data (REQUIRED)
# Generate with: openssl rand -hex 64
# Must be at least 64 characters (512 bits) for AES-256 security
ENCRYPTION_MASTER_KEY=generate-a-secure-64-character-hex-key-using-openssl-rand-hex-64

# Rate limiting configuration
RATE_LIMIT_ENABLED=true
RATE_LIMIT_MAX_REQUESTS=100
RATE_LIMIT_WINDOW_MS=900000

# Session security (in seconds)
SESSION_MAX_AGE=7200

# Demo mode (ONLY for development - set to false in production)
ALLOW_DEMO_MODE=true

# =============================================================================
# Redis Configuration (Optional - improves performance and scalability)
# =============================================================================

# Redis URL for caching and distributed rate limiting
# REDIS_URL=redis://localhost:6379
# REDIS_URL=redis://user:password@host:6379

# =============================================================================
# OAuth Provider Configuration (Optional)
# =============================================================================

# Google OAuth
# GOOGLE_CLIENT_ID=your-google-client-id
# GOOGLE_CLIENT_SECRET=your-google-client-secret

# GitHub OAuth
# GITHUB_CLIENT_ID=your-github-client-id
# GITHUB_CLIENT_SECRET=your-github-client-secret

# =============================================================================
# LLM Provider API Keys (Optional - can be set in UI)
# =============================================================================

# OpenAI
# OPENAI_API_KEY=sk-your-openai-api-key

# Anthropic (Claude)
# ANTHROPIC_API_KEY=sk-ant-your-anthropic-api-key

# Google AI (Gemini)
# GOOGLE_AI_API_KEY=your-google-ai-api-key

# OpenRouter (Multiple models)
# OPENROUTER_API_KEY=sk-or-your-openrouter-api-key

# =============================================================================
# Monitoring and Logging (Optional)
# =============================================================================

# Sentry for error monitoring
# SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id

# Log level (error, warn, info, debug)
LOG_LEVEL=info

# =============================================================================
# Feature Flags
# =============================================================================

# Enable analytics tracking
ENABLE_ANALYTICS=true

# Enable telemetry
ENABLE_TELEMETRY=true

# =============================================================================
# Development Only Settings
# =============================================================================

# Vitest test environment
# VITEST=false

# =============================================================================
# Production Deployment Settings
# =============================================================================

# When deploying to production, ensure:
# 1. NODE_ENV=production
# 2. ENCRYPTION_MASTER_KEY is set to a secure 64-character hex string
# 3. NEXTAUTH_SECRET is set to a secure 32+ character string
# 4. ALLOW_DEMO_MODE=false
# 5. DATABASE_URL points to your production database
# 6. All OAuth secrets are properly configured
# 7. SESSION_MAX_AGE is appropriately short (7200 = 2 hours recommended)
# 8. Rate limiting is enabled and configured appropriately
# 9. Monitoring (SENTRY_DSN) is configured

# =============================================================================
# Security Checklist for Production
# =============================================================================

# ✅ ENCRYPTION_MASTER_KEY is 64+ characters and randomly generated
# ✅ NEXTAUTH_SECRET is 32+ characters and randomly generated
# ✅ ALLOW_DEMO_MODE=false
# ✅ SESSION_MAX_AGE <= 7200 (2 hours or less)
# ✅ RATE_LIMIT_ENABLED=true
# ✅ All API keys are properly secured and rotated regularly
# ✅ Database credentials are secure
# ✅ OAuth secrets are secure and properly configured
# ✅ Monitoring is enabled
# ✅ Log level is appropriate for production (info or warn)

# =============================================================================
# Environment-Specific Examples
# =============================================================================

# Development Example:
# NODE_ENV=development
# NEXTAUTH_URL=http://localhost:3000
# ALLOW_DEMO_MODE=true
# SESSION_MAX_AGE=7200

# Production Example:
# NODE_ENV=production
# NEXTAUTH_URL=https://yourdomain.com
# ALLOW_DEMO_MODE=false
# SESSION_MAX_AGE=3600
# DATABASE_URL=postgresql://...
# REDIS_URL=redis://...
# SENTRY_DSN=https://...

# =============================================================================
# Key Generation Commands
# =============================================================================

# Generate ENCRYPTION_MASTER_KEY:
# openssl rand -hex 64

# Generate NEXTAUTH_SECRET:
# openssl rand -base64 32

# Generate session secret:
# node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"